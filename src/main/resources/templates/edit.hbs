{{#partial "title"}}Signed in{{/partial}}

{{#partial "content"}}

   <section>
        <div class="container">
            <div class="edit-entry">
                <h2>Edit Entry</h2>
                <form action="/detail/{{ detail.slug }}/edit" method="put">
                    <label for="title"> Title</label>
                    <input type="text" name="title"><br>
                    <label for="creator"> Name</label>
                    <input type="text" name="creator"><br>
                    <label for="entry">Entry</label>
                    <textarea rows="5" name="blogEntry"></textarea>
                    <input type="submit" value="Save Entry" class="button">
                    <a href="/" class="button button-secondary">Cancel</a>
                </form>
            </div>
        </div>
    </section>

{{/partial}}

{{> base.hbs}}